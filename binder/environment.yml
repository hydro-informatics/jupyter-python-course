name: hy-jupyter
channels:
  - conda-forge
  - nodefaults

# TEMPORARY minimal environment to diagnose the Binder spawn timeout
# ("Server didn't respond in 30 seconds"). If this branch launches fine,
# the full environment's size is the cause.

dependencies:
  - python=3.11

  # Jupyter runtime
  - ipykernel
  - ipywidgets

  # Core scientific stack
  - numpy
  - pandas
  - matplotlib
